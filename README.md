# UUIDGenerator
Generates a UUID at localhost:4545.
The supported platform is Linux / amd64 only.

## Usage
1. Open the terminal
2. Run `git clone https://github.com/tarutarudev/uuidGen.git && cd uuidGen && RUSTFLAGS='-C target-cpu=native' cargo run --release`
3. Open localhost:4545
